const prompt = require('prompt-sync')();

let length=prompt("enter width of the rectangle:");
let width=prompt("enter width of the rectangle:")

let rectangle=length*width;

console.log("area of the rectangle is:" + rectangle);
